- clean: ['~']

- link:
    ~/.dotfiles: ''
    ~/.tmux.conf: tmux.conf
    ~/.vim: vim/
    ~/.vimrc: vimrc
    ~/.hushlogin: hushlogin
    ~/.oh-my-zsh: oh-my-zsh
    ~/.bin: bin/

- shell:
  - [git submodule update --init --recursive, Installing submodules]
