# use zsh
set-option -g default-shell /bin/zsh

# set vi mode and allow reload with 'r'
setw -g mode-keys vi
# setw -g mode-mouse on
unbind r
bind r source-file ~/.tmux.conf\; display "Reloading Matrix..."

# allow terminal scrolling
set-option -g terminal-overrides 'xterm*:smcup@:rmcup@'

# vim style copy paste mode
#unbind [
bind Escape copy-mode
unbind p
bind p paste-buffer
bind-key -T copy-mode-vi 'v' send -X begin-selection
bind-key -T copy-mode-vi 'y' send -X copy-selection-and-cancel

# rebind previous and next window keys to '[' and ']'
#bind [ previous-window
#bind ] next-window

# improve look
set -g default-terminal "screen-256color"

# don't rename windows automatically
set -g allow-rename off


bind j select-pane -D
bind k select-pane -U
bind h select-pane -L
bind l select-pane -R

bind | split-window -h
bind - split-window -v

bind -r J resize-pane -D 5
bind -r K resize-pane -U 5
bind -r H resize-pane -L 5
bind -r L resize-pane -R 5

#
# ######################
# ### DESIGN CHANGES ###
# ######################
# 
# The statusbar
set -g status-position bottom
set -g status-bg color143
set -g status-fg color237
set -g status-left ''
set -g status-right '#[fg=colour233,bg=colour139,bold] %m/%d %H:%M:%S '
set -g status-right-length 50
set -g status-left-length 20
set -g window-style 'fg=colour247,bg=color236'
set -g window-active-style 'fg=color250,bg=black'
