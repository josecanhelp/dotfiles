# List of plugins
set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'tmux-plugins/tmux-resurrect'
set -g @plugin 'tmux-plugins/tmux-continuum'
set -g @plugin 'tmux-plugins/tmux-copycat'
set -g @plugin 'tmux-plugins/tmux-yank'

# Plugin configurations
set -g @continuum-boot 'on'
set -g @continuum-boot-options 'iterm'
set -g @continuum-restore 'on'
set -g @resurrect-capture-pane-contents 'on'

# Use ZSH
set-option -g default-shell /bin/zsh

# Change prefix to ctrl+a
set -g prefix C-a
unbind-key C-b
bind-key C-a send-prefix

# Synchonize panes
bind s setw synchronize-panes

# Set vi mode
setw -g mode-keys vi

# Reload tmux configuration with 'r'
unbind r
bind r source-file ~/.tmux.conf\; display "Reloading Matrix..."

# Allow terminal scrolling
set-option -g terminal-overrides 'xterm*:smcup@:rmcup@'

# Improve look
set -g default-terminal "screen-256color"

# Don't rename windows automatically
# set -g allow-rename off

# Easier window splitting
bind | split-window -h -c "#{pane_current_path}"
bind - split-window -v -c "#{pane_current_path}"
bind c new-window -a

# Resize panes
bind j select-pane -D
bind k select-pane -U
bind h select-pane -L
bind l select-pane -R
bind -r J resize-pane -D 5
bind -r K resize-pane -U 5
bind -r H resize-pane -L 5
bind -r L resize-pane -R 5


if-shell "test -f ~/dotfiles/tmux/tmuxline" "source ~/dotfiles/tmux/tmuxline"

# ------------- #
# Define Colors #
# ------------- #
# LIGHT_GRAY="colour59"
# BLACK="colour0"
# DARK_BLUE="colour17"
# GRAY="colour239"
# RED="colour1"
# TEAL="colour2"
# YELLOW="colour220"

# ----------------- #
# Define Status Bar #
# ----------------- #
# set -g status-interval 1
# set -g status-style bg=$TEAL,fg=$LIGHT_GRAY

# ------------ #
# Define Panes #
# ------------ #
# set -g pane-border-style bg=$BLACK,fg=$LIGHT_GRAY
# set -g pane-active-border-style bg=$BLACK,fg=$TEAL

# ---------------- #
# Define Left Side #
# ---------------- #
# set -g status-left " "

# -------------- #
# Define Windows #
# -------------- #
# set -g window-status-current-format "#[fg=$RED,bg=$TEAL]#{?window_zoomed_flag,ᐉ,ᐅ}"
# set -g window-status-format "#{?window_zoomed_flag,ᐉ,ᐅ}"
# set -g window-status-activity-style fg=$RED
# set -g window-status-bell-style fg=$YELLOW
# set -g window-status-separator " "
# set -g window-status-style bg=$TEAL,fg=$TEAL
# set -g window-style bg=$BLACK,fg=$LIGHT_GRAY
# set -g window-active-style bg=$BLACK,fg=$TEAL

# ----------------- #
# Define Right Side #
# ----------------- #
# set -g status-right-length 80
# set -g status-right "#{?client_prefix,#[reverse]:#[noreverse] ,}#W "

# Initialize TMUX plugin manager (keep this line at the very bottom of tmux.conf)
run -b '~/.tmux/plugins/tpm/tpm'
